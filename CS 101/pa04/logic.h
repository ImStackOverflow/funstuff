#ifndef poopybutthole
#define poopybutthole

#include "minPQ.h"
#include "adjWgtVec.h"
#include "loadGraph.h"


//algorithim for primms or djikstras shit
//input: populated data struct with filled adj array and empty priority queue, start vertex, algorithim flag
//output: none
//it calculates and fills the priority queue ADT depending on whether flag is for prims or djikistras
void algorithm(data poof, int start, Flag flag);

#endif
